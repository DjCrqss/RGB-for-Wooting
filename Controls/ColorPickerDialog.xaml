<Window x:Class="WootingRGB.Controls.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="380" Width="260"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent">

    <Window.Resources>
        <!-- Hue Slider Style -->
        <Style x:Key="HueSliderStyle" TargetType="Slider">
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Opacity="0" Command="Slider.DecreaseLarge"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Opacity="0" Command="Slider.IncreaseLarge"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="14" Height="14">
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Ellipse Fill="White" Stroke="#888888" StrokeThickness="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Recent Color Button Style -->
        <Style x:Key="RecentColorButtonStyle" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="14"
                                BorderBrush="#555555"
                                BorderThickness="1">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#2B2B2B" CornerRadius="8" BorderBrush="#404040" BorderThickness="1" Margin="0,0,0,-60">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="190"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 2D Color Canvas -->
            <Border Grid.Row="0" BorderBrush="#404040" BorderThickness="1" CornerRadius="6" Margin="0,0,0,10">
                <Grid x:Name="ColorCanvas" 
                      Background="Transparent"
                      MouseLeftButtonDown="ColorCanvas_MouseDown" 
                      MouseMove="ColorCanvas_MouseMove"
                      MouseLeftButtonUp="ColorCanvas_MouseUp">
                    <Rectangle x:Name="HueGradient" IsHitTestVisible="False">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="White" Offset="0"/>
                                <GradientStop x:Name="HueColor" Color="Red" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <Rectangle IsHitTestVisible="False">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="Black" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <!-- Picker Circle -->
                    <Ellipse x:Name="PickerCircle" Width="12" Height="12" 
                             Stroke="White" StrokeThickness="1.5" 
                             IsHitTestVisible="False"
                             HorizontalAlignment="Left" VerticalAlignment="Top"/>
                </Grid>
            </Border>

            <!-- Hue Slider -->
            <Grid Grid.Row="1" Height="22" Margin="0,0,0,10">
                <Border CornerRadius="6" Height="12">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                            <GradientStop Color="#FF0000" Offset="0.00"/>
                            <GradientStop Color="#FFFF00" Offset="0.17"/>
                            <GradientStop Color="#00FF00" Offset="0.33"/>
                            <GradientStop Color="#00FFFF" Offset="0.50"/>
                            <GradientStop Color="#0000FF" Offset="0.67"/>
                            <GradientStop Color="#FF00FF" Offset="0.83"/>
                            <GradientStop Color="#FF0000" Offset="1.00"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Slider x:Name="HueSlider" Minimum="0" Maximum="360" 
                        VerticalAlignment="Center" 
                        ValueChanged="HueSlider_ValueChanged"
                        Style="{StaticResource HueSliderStyle}"/>
            </Grid>

            <!-- Hex Input -->
            <Grid Grid.Row="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="HEX" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="8,0,0,0" FontSize="10"/>
                <Border Grid.Column="1" Background="#404040" CornerRadius="3" Padding="8,6" Margin="10,0,0,0">
                    <TextBox x:Name="HexTextBox" Background="Transparent" Foreground="White" 
                             BorderThickness="0" FontFamily="Consolas" FontSize="11"
                             TextChanged="HexTextBox_TextChanged"/>
                </Border>
            </Grid>

            <!-- Recently Used -->
            <StackPanel Grid.Row="3">
                <TextBlock Text="RECENTLY USED" Foreground="#AAAAAA" FontSize="9" Margin="0,0,0,6"/>
                <UniformGrid x:Name="RecentColorsGrid" Rows="2" Columns="6" Height="75">
                    <!-- Recent colors will be added here programmatically -->
                </UniformGrid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
