<UserControl x:Class="WootingRGB.Controls.ToggleSwitch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="60">
    <Grid>
        <Border x:Name="TrackBorder"
                Width="50"
                Height="26"
                CornerRadius="13"
                Background="#3E3E42"
                BorderBrush="#555555"
                BorderThickness="1"
                Cursor="Hand"
                MouseLeftButtonDown="Toggle_Click">
            <Grid>
                <!-- Thumb (the circle that slides) -->
                <Ellipse x:Name="Thumb"
                         Width="18"
                         Height="18"
                         Fill="#E0E0E0"
                         HorizontalAlignment="Left"
                         Margin="4,0,0,0">
                    <Ellipse.RenderTransform>
                        <TranslateTransform x:Name="ThumbTransform" X="0"/>
                    </Ellipse.RenderTransform>
                </Ellipse>
                
                <!-- Checkmark icon (only visible when on) -->
                <Path x:Name="CheckMark"
                      Data="M 4 8 L 7 11 L 12 5"
                      Stroke="#1E1E1E"
                      StrokeThickness="2"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      Margin="6,0,0,0"
                      Opacity="0">
                    <Path.RenderTransform>
                        <TranslateTransform x:Name="CheckMarkTransform" X="0"/>
                    </Path.RenderTransform>
                </Path>
            </Grid>
        </Border>
    </Grid>
</UserControl>
